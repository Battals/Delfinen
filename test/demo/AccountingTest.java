package demo;

//import org.junit.jupiter.api.Test;

import java.time.LocalDate;

//import static org.junit.jupiter.api.Assertions.*;

/*class AccountingTest {

@Test
    public void testPayDebt(){
    //Arrange
    Accounting accounting = new Accounting("acc", "1234");
    Member member = new Member(1, "test man", LocalDate.of(1970, 1, 2), true);
    accounting.addMonthlyDebt(member);
    double firstDebt = member.getDebt();

    //Act
    double payment = 1000;
    accounting.payDebt(member, payment);
    double newDebt = member.getDebt();

    //Assert
    assert((newDebt - 1000) == firstDebt);
}*/